---

- name: configure and deploy the databases
  remote_user: root
  hosts: dbservers
  roles:
    - base
    - dbservers

- name: configure and deploy the webservers
  remote_user: root
  hosts: webservers
  roles:
    - base
    - webservers
